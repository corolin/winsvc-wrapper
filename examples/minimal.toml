# The minimal viable service definition — everything else is defaulted.
[service]
id = "myapp"

[process]
executable = "app.exe"
arguments = ["--port", "8080"]

[env]
DATABASE_URL = "postgres://user:pass@localhost/db"
